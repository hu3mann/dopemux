# Dopemux Color Protocol

Use ANSI escape codes for neon effects. Default styles:
- `dopamine`: cyan
- `filth`: magenta

Wrap banners with `utils.colorize()` if printing from Dopemux CLI.
Override colors in `config.yaml` under `dopemux.colors`.
